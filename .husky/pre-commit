# Run tests before commit (exclude Angular libs missing jest-preset-angular until migration cleanup)
echo "Running tests before commit..."
npx nx run-many --target=test --all --exclude=DataAccess,ui-components
